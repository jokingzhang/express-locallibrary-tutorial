import React, {FC} from 'react';

interface IProps {
    className?: string;
}

const NotFound: FC<IProps> = props => {
    return (
        <div>
            NotFound
        </div>
    )
}

export default NotFound;